.comments-box
  %div
    %h6
      = t('comments.posted')
      = time_ago_in_words(comment.created_at)
      = t('comments.ago')
      %br
      = t('comments.sender')
      = comment.user.email
      %br
      =comment.body
